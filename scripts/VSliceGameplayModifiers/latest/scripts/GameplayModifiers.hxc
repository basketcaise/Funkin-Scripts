import Std;
import Math;

import flixel.FlxG;
import funkin.save.Save;
import flixel.text.FlxText;

import openfl.text.TextField;
import openfl.text.TextFormat;

import funkin.Conductor;
import funkin.Preferences;

import funkin.ui.options.OptionsState;
import funkin.ui.options.PreferencesMenu;

import funkin.modding.module.Module;
import funkin.modding.events.ScriptEvent;

import funkin.util.Constants;
import funkin.play.PlayState;

class GameplayModifiers extends Module {
	var save:Save;
	var modOptions:Map;
	var debugTxt:TextField;
		
	public function new() {
		super('gameplay-modifiers');
		save = Save.instance;
		
		if (save.modOptions.get('GameplayModifiers') == null) {
			var default:Map<String, Dynamic> = [
				'botplay' => false,
				'minimal' => false,
				
				'scrollSpeed' => 0.0,
				'playbackRate' => 1.0,
				
				'restartModifiers' => false,
				'killOnMiss' => false,
				'killOnGhostTap' => false,
				'perfectMode' => false,
				
				'debug' => false
			];
			
			save.modOptions.set('GameplayModifiers', default);
			save.flush();
		}
		
		modOptions = save.modOptions.get('GameplayModifiers');
		
		debugTxt = new TextField();
		debugTxt.x = 10;
		debugTxt.y = 33;
		
		debugTxt.width = FlxG.width;
		debugTxt.height = FlxG.height;
		debugTxt.defaultTextFormat = new TextFormat("_sans", 12, 0xFFFFFF, false, false, false, '', '', 'left');
		
		debugTxt.selectable = false;
		debugTxt.mouseEnabled = false;
	}
	
	function onCreate(event) {
		super.onCreate(event);
		if (Preferences.debugDisplay && modOptions.get('debug')) FlxG.stage.addChild(debugTxt);
	}
	
	function onStateChangeEnd(event) {
		if (Std.isOfType(event.targetState, OptionsState)) {
			var state = event.targetState;
			var options = state.optionsCodex.pages.get('options');
			
			// modifier menu itself
			var modifiers:PreferencesMenu = state.optionsCodex.addPage('Modifiers', new PreferencesMenu());
			
			for (i in modifiers.items) i.destroy();
			for (i in modifiers.preferenceItems) i.destroy();
			
			modifiers.items.clear();
			modifiers.preferenceItems.clear();
			modifiers.preferenceDesc = [];
			
			modifiers.onExit.add(function() state.optionsCodex.switchPage('options'));
			options.createItem('MODIFIERS', function() options.codex.switchPage('Modifiers'));
			options.items.members[options.items.length - 1].y = 0;
			
			// STATES
			modifiers.items.createItem(0, (120 * modifiers.items.length) + 30, '/ State Modifiers /', 'bold', function() { trace('HAWK TUAH'); });
			modifiers.preferenceDesc.push('Modifiers that affect PlayState itself');
			
			modifiers.createPrefItemCheckbox("Bot Play Mode", "Songs start with Bot Play Mode enabled", function(value:Bool) {
                modOptions.set('botplay', value);
            }, modOptions.get('botplay'));
			
			/* can't seem to get this to work properly
			modifiers.createPrefItemCheckbox("Minimal Mode", "Songs start with Minimal Mode enabled", function(value:Bool) {
                modOptions.set('minimal', value);
            }, modOptions.get('minimal'));
			*/
			
			// NUMERICALS
			modifiers.items.createItem(0, (120 * modifiers.items.length) + 30, '/ Numerical Modifiers /', 'bold', function() { trace('HAWK TUAH'); });
			modifiers.preferenceDesc.push('Modifiers that affect numerical values in a song');
			
			modifiers.createPrefItemNumber('Scroll Speed', 'Sets a scroll speed to be used across all levels, 0 sets it to chart default', function(value:Float) {
				modOptions.set('scrollSpeed', value);
			}, null, modOptions.get('scrollSpeed'), 0, 10, 0.05, 2);
			
			modifiers.createPrefItemNumber('Playback Rate', 'Sets a playback rate to be used across all levels', function(value:Float) {
				modOptions.set('playbackRate', value);
			}, null, modOptions.get('playbackRate'), 0.05, 10, 0.05, 2);
			
			// JUDGEMENTALS
			modifiers.items.createItem(0, (120 * modifiers.items.length) + 30, '/ Judgement Modifiers /', 'bold', function() { trace('HAWK TUAH'); });
			modifiers.preferenceDesc.push('Modifiers that affect gameplay based on your note judgements');
			
			modifiers.createPrefItemCheckbox("Restart instead of Kill", "All Instakill modifers restart the song instead of killing you", function(value) {
                modOptions.set('restartModifiers', value);
            }, modOptions.get('restartModifiers'));
			
			modifiers.createPrefItemCheckbox("Instakill on Miss", "Missing a note kills you", function(value:Bool) {
                modOptions.set('killOnMiss', value);
            }, modOptions.get('killOnMiss'));
			
			modifiers.createPrefItemCheckbox("Instakill on Ghost Tap", "Pressing the strums while no note is present kills you", function(value:Bool) {
                modOptions.set('killOnGhostTap', value);
            }, modOptions.get('killOnGhostTap'));
			
			modifiers.createPrefItemCheckbox("Perfect Mode", "Getting anything other than 'Sick!!' as your judgement kills you", function(value:Bool) {
               modOptions.set('perfectMode', value);
            }, modOptions.get('perfectMode'));
			
			// DEBUG
			modifiers.items.createItem(0, (120 * modifiers.items.length) + 30, '/ Debug /', 'bold', function() { trace('HAWK TUAH'); });
			modifiers.preferenceDesc.push('debug lol');
			
			modifiers.createPrefItemCheckbox("Debug Info", "Toggles debug information regarding the modifiers, Debug Display in Preferences needs to be enabled as well", function(value:Bool) {
				if (Preferences.debugDisplay && value)
					FlxG.stage.addChild(debugTxt);
				else
					FlxG.stage.removeChild(debugTxt);
				
				modOptions.set('debug', value);
            }, modOptions.get('debug'));
			
			save.flush();
		}
		
		if (Preferences.debugDisplay && modOptions.get('debug') && debugTxt != null) debugTxt.text = 'Botplay: ' + modOptions.get('botplay') + '\nMinimal: ' + modOptions.get('minimal') + '\n\nScroll Speed: ' + modOptions.get('scrollSpeed') + '\nPlayback Rate: ' + modOptions.get('playbackRate') + '\n\nRestart Modifiers: ' + modOptions.get('restartModifiers') + '\nKill on Miss: ' + modOptions.get('killOnMiss') + '\nKill on Ghost Tap: ' + modOptions.get('killOnGhostTap') + '\nPerfect Mode: ' + modOptions.get('perfectMode');
	}
	
	function onCountdownStart(event) {
		super.onCountdownStart(event);
		PlayState.instance.isBotPlayMode = modOptions.get('botplay');
		// PlayState.instance.isMinimalMode = modOptions.get('minimal');
		PlayState.instance.playbackRate = modOptions.get('playbackRate');
		
		PlayState.instance.playerStrumline.scrollSpeed = (modOptions.get('scrollSpeed') != 0 ? modOptions.get('scrollSpeed') : PlayState.instance.currentChart.scrollSpeed) / PlayState.instance.playbackRate;
		PlayState.instance.opponentStrumline.scrollSpeed = (modOptions.get('scrollSpeed') != 0 ? modOptions.get('scrollSpeed') : PlayState.instance.currentChart.scrollSpeed) / PlayState.instance.playbackRate;
	}
	
	function onNoteHit(event:HitNoteScriptEvent) {
		super.onNoteHit(event);
		if (modOptions.get('perfectMode') && event.note.noteData.getMustHitNote() && event.judgement != "sick" && !PlayState.instance.isPracticeMode) {
			if (modOptions.get('restartModifiers'))
				PlayState.instance.needsReset = true;
			else
				PlayState.instance.health = Constants.HEALTH_MIN - Constants.HEALTH_MIN;
		}
	}
	
	function onNoteMiss(event:HitNoteScriptEvent) {
		super.onNoteGhostMiss(event);
		if ((modOptions.get('perfectMode') || modOptions.get('killOnMiss')) && !PlayState.instance.isPracticeMode) {
			if (modOptions.get('restartModifiers'))
				PlayState.instance.needsReset = true;
			else
				PlayState.instance.health = Constants.HEALTH_MIN - Constants.HEALTH_MIN;
		}
	}
	
	function onNoteGhostMiss(event:HitNoteScriptEvent) {
		super.onNoteGhostMiss(event);
		if ((modOptions.get('perfectMode') || modOptions.get('killOnGhostTap')) && !PlayState.instance.isPracticeMode) {
			if (modOptions.get('restartModifiers'))
				PlayState.instance.needsReset = true;
			else
				PlayState.instance.health = Constants.HEALTH_MIN - Constants.HEALTH_MIN;
		}
	}
}
